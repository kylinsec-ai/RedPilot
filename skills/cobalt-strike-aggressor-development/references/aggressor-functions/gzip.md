gzip

GZIP a string.

#### Arguments

`$1` - the string to compress

#### Returns

The argument processed by the gzip compressor

#### Example

```
println(gzip("this is a test"));```

See also&gunzip
