pi_user_explicit_get

gets the 'Name' for the actively selected user-defined explicit injection.

#### Returns

A string name of the selected user-defined explicit injection. Null if no user-defined selection is currently selected.

#### Example

```
println(pi_user_explicit_get());```
