-is64

Check if a session is on an x64 system or not (Beacon only).

#### Arguments

`$1` - Beacon/Session ID

#### Example

```
command x64 {
   foreach $session (beacons()) {
      if (-is64 $session['id']) {
         println($session);
      }
   }
}```
