pi_explicit_info

Get a list of all built-in explicit injections available.

#### Returns

An array containing the names of all built-in explicit injections available.

#### Example

```
println(pi_explicit_info());```
