pi_spawn_info

Get a list of all built-in spawn injections available.

#### Returns

An array containing the names of all built-in spawn injections available.

#### Example

```
println(pi_spawn_info());```
