pi_explicit_get

Gets the active selection for built-in explicit injection.

#### Returns

A string name of the selected built-in explicit injection.

#### Example

```
println(pi_explicit_get());```
