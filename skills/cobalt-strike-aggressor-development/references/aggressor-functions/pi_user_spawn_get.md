pi_user_spawn_get

gets the 'Name' for the actively selected user-defined spawn injection

#### Returns

A string name of the selected user-defined spawn injection. Null if no user-defined selection is currently selected.

#### Example

```
println(pi_user_spawn_get());```
