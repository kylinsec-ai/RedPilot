interface:
  display_name: "GitHound / OpenHound GitHub"
  short_description: "Analyze GitHub OpenGraph identity and Actions paths."
  icon_small: ./assets/icon.svg
  icon_large: ./assets/icon.png
  default_prompt: "Use $openhound-github to analyze GitHound/OpenHound GitHub organizations, repositories, teams, branch protection, Actions policy, secrets metadata, SAML/SCIM external identities, PATs, and OIDC exposure."
  brand_color: "#991B1B"

policy:
  allow_implicit_invocation: true
