interface:
  display_name: "SSH Ops"
  short_description: "Operate hosts over SSH safely"
  icon_small: ./assets/icon.svg
  icon_large: ./assets/icon.png
  brand_color: '#00B36B'
  default_prompt: "Use $ssh-ops to establish and operate scoped SSH sessions with safe prechecks, command/output evidence capture, and OPSEC-aware escalation."
