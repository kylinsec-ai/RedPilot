# 基础模型实验室的智能体工程最佳实践

## Anthropic · OpenAI · Google DeepMind 深度研究报告

> 资料截止：2026 年 9 月 17 日
> 研究范围：三家前沿实验室的官方工程博客、产品文档、开发者指南、年度趋势报告，并对照第三方生产数据（LangChain、Datadog）与安全披露（Black Hat USA 2026）

---

## 摘要：十二条核心结论

1. **范式已经换名。** 三家实验室的官方口径一致地把重心从 *prompt engineering* 转向 **context engineering**（上下文工程）。Anthropic 的定义：在有限的上下文窗口里，从不断膨胀的"可能信息宇宙"中策展出最优的 token 集合。

2. **架构的默认答案变了。** Anthropic、OpenAI、Google、Microsoft 四家都公开建议**先做单智能体**，把多智能体当作需要被论证的例外，而不是默认架构。

3. **上下文是有限资源，不是越多越好。** 共识概念是 **context rot**（上下文腐化）与 **attention budget**（注意力预算）：token 越多，模型精确回忆能力越差，性能呈边际递减。

4. **"少即是多"成为新默认。** Anthropic 在 Claude 5 代模型上删掉了 Claude Code **超过 80% 的系统提示**，编码评测无可测量损失。四组规则迁移：给规则→给判断力，给示例→设计接口，全量前置→渐进式披露，重复强调→精简描述。

5. **渐进式披露（progressive disclosure）成为跨厂商的第一设计原则。** Agent Skills（`SKILL.md`）三层加载模型在 2025 年 12 月开放为标准后，数周内被 OpenAI Codex、Google Gemini CLI、GitHub Copilot、Cursor 等采纳，2026 年已有 40+ 产品实现。

6. **工具设计是投入产出比最高的单点改进。** Anthropic 2024 年只改工具描述就把 Sonnet 3.5 推上 SWE-bench SOTA；2026 年的结论是"**Agent 的效果上限，等于它被给予的工具的效果上限**"。

7. **长任务的成熟解法是"三件套 + 一次重置"**：compaction（压缩）、structured note-taking / memory（外部记忆）、sub-agent（上下文隔离），再叠加 **context reset + 结构化交接**——因为 compaction 无法消除模型的"上下文焦虑"。

8. **多智能体的真实收益来自并行与隔离，而不是"更聪明"。** Anthropic 内部评测多智能体比单智能体高 **90.2%**，但 token 消耗约为 **15 倍**；在 BrowseComp 上，仅 token 使用量就解释了 **80%** 的性能方差。

9. **评估是可观测性的下半场。** LangChain 2026 调研：**57%** 的组织已有生产环境 agent，**89%** 做了可观测性，但只有 **52%** 跑离线评测、**37%** 跑在线评测。**质量（32%）而非成本**，是头号落地障碍。

10. **评估对象是"轨迹"而非"答案"。** OpenAI 给出 Trace / Graders / Datasets / Eval Runs 四件套并延伸出 macro-eval（跨轨迹聚类失败模式）；Anthropic 提倡尽早开始 eval-driven development（20–50 个任务即可起步）并用 **pass^k** 而非 pass@k 衡量生产可靠性。

11. **安全在 2026 年从"注意事项"升级为"工程硬约束"。** Black Hat USA 2026 集中披露了 Claude Code、Gemini CLI、Codex 在 harness 层的漏洞（含 CVE-2026-54316）；Anthropic 发布《Zero Trust for AI Agents》白皮书。共识：**假设一定会被攻破，把边界放在"动作"上，而不是放在提示词上。**

12. **协议层在 2026 年定格为三层。** MCP 在 2026-07-28 完成史上最大改版（无状态核心）；A2A 在 Linux Foundation 下发布 v1.0（2026-03-12）；Agent Skills 成为开放标准。分工：**MCP 接工具，A2A 接 agent，Skills 装流程。**

**五处实质性分歧**见第十一章。

---

## 一、格局：三家实验室的产品面与工程主张

| 维度 | Anthropic | OpenAI | Google（DeepMind + Cloud） |
| --- | --- | --- | --- |
| 旗舰模型（2026） | Claude Opus 5 / Fable 5 | GPT-5.6（含 terra / luna 轻量变体） | Gemini 3.x（3.1 Pro / 3.8 Flash） |
| Agent 运行时 | Claude Code + Claude Agent SDK | Responses API + Agents SDK + hosted shell | Gemini Enterprise Agent Platform + ADK + Agent Runtime |
| 技能机制 | Agent Skills（**原始作者**，开放标准） | Codex Skills / ChatGPT Skills（同一标准） | ADK `SkillToolset` + Gemini CLI |
| 工具接入 | MCP（**原始作者**） | MCP + Connector Registry | MCP + 云服务原生集成 |
| Agent 间通信 | 以 MCP 生态为主 | Agents SDK handoffs | **A2A**（原始作者，Linux Foundation） |
| 评估体系 | evals + early eval-driven dev | Evals / trace grading / macro-eval | Agent Evaluation + Observability + Simulation |
| 治理与安全 | Zero Trust for AI Agents | Guardrails（开源）+ Lockdown Mode | Agent Identity / Registry / Gateway + 异常检测 |
| 2026 年的战略动作 | 淡化系统提示、力推 Skills 与渐进式披露 | 裁撤可视化 Agent Builder，回归 Agents SDK 与代码优先 | 把 Vertex AI 全部服务收拢进 Agent Platform |

**一句话读法**：Anthropic 在卖"**harness 工程学**"（把 agent 做稳的方法论），OpenAI 在卖"**受控的执行环境 + 评测闭环**"，Google 在卖"**企业级治理与互操作标准**"。三家的技术结论高度趋同，商业落点各不相同。

---

## 二、架构选型：单智能体优先，多智能体是"挣来的复杂度"

### 2.1 四家口径一致地劝退默认多智能体

- **Anthropic**：明确要求"**最简单的可行方案**"，很多应用只需一次 LLM 调用 + 检索 + 少量示例即可；并特别警告 agent 框架的抽象层会遮蔽 prompt 与 response，让系统更难调试。
- **OpenAI**：先把**单个 agent 的能力最大化**；在架构扩张之前，优先尝试 prompt 模板与 persona 切换。
- **Microsoft**：除非用例跨越明确的安全边界、合规域或团队所有权，否则先做单 agent 原型；点名"planner / reviewer / executor"**不足以**构成拆成多个 agent 的理由——一个 agent 通过 persona 切换与工具权限即可模拟。
- **Google Cloud**：选错"sub-agent 还是把 agent 打包成工具"，会带来随时间累积的额外开销。有时你需要的不是多一个队友，**而是一个契约清晰的函数**。

这被业界类比为"**microservices 债**"：多智能体并没有消除复杂度，只是把它搬到了 handoff、prompt 仲裁、上下文同步与 agent 状态里。

### 2.2 官方认可的模式清单（Anthropic 五种工作流）

| 模式 | 形态 | 适用 |
| --- | --- | --- |
| Augmented LLM | 模型 + 检索 + 工具 + 记忆 | 所有 agent 的原子单元 |
| Prompt chaining | 固定线性步骤，步骤间有程序化门禁 | 可清晰分解、要求高确定性的任务 |
| Routing | 先分类，再分流到专用下游 | 输入类别明确、各类处理差异大 |
| Parallelization | 并行执行无依赖子任务后汇总 | 可切分、追求时延 |
| Orchestrator–workers | 中央 LLM 动态拆解任务、分派 worker、汇总 | 子任务无法预先枚举 |
| Evaluator–optimizer | 一个生成、一个评估并反馈、循环迭代 | 有明确评价标准、迭代能收敛 |

在此之上才是 **autonomous agent**：模型自己决定下一步动作，循环"计划→行动→校验→修正"。

### 2.3 多智能体的真实收益与代价（Anthropic 生产数据）

Anthropic 的 Research 功能是业界最完整的公开案例：lead agent 分析查询、把计划写入外部记忆、派生并行 subagent、每个 subagent 用 interleaved thinking 评估工具结果，最后由专门的 **CitationAgent** 定位引用位置。

- 多智能体（Opus 4 lead + Sonnet 4 sub）比单智能体 Opus 4 在内部研究评测上高 **90.2%**；
- 但在 BrowseComp 上，**token 使用量单独解释了 80% 的性能方差**，工具调用数与模型选择是另外两个因子；
- 成本约为普通对话交互的 **15 倍**；
- 优势集中在 **breadth-first、多独立方向**的查询上（如"找出标普 500 信息科技公司全部董事会成员"），单智能体在此类任务上因串行搜索而失败。

**结论**：多智能体不是"更聪明"，而是"**用更多 token 换取并行探索与上下文隔离**"。

### 2.4 反方与修正：Cognition 的立场演进

- **2025《Don't Build Multi-Agents》**：平行 subagent 各自对风格、边界情况、实现模式做**隐式决策**，这些决策互相冲突；作者给出两条原则——**共享完整上下文（而非摘要）**、**行动携带隐式决策**。替代方案是单线程线性 agent + 专门的上下文压缩模型。
- **2026《Multi-Agents: What's Actually Working》**：修正为——**让多个 agent 贡献"智能"（读、分析、检索），但写入（改变状态的动作）保持单线程**。这与 Anthropic 的实践方向一致：只读 subagent 是主流，写动作要收敛。

### 2.5 何时该用 subagent：Anthropic 的判定规则

**该用**：结果可压缩到几行；中间过程超过 10 轮循环；任务可并行；需要比主 agent 更专的提示词（如安全审计）；主上下文已接近上限。

**不该用**：主 agent 与 subagent 需要频繁往返；中间结果不可压缩且主 agent 需引用原始数据；任务只有一两步。

**工程细节**（Claude Agent SDK）：subagent 是**完全隔离的子进程**，拥有自己的文件系统上下文；`isolation: "worktree"` 为其创建临时 git worktree，无提交则自动清理，有提交则保留供父 agent 审查合并。主 agent 只收到一条 `tool_result`，中间 30 次 Read 不污染主历史。嵌套深度一般安全在两层，更深常失败。

---

## 三、上下文工程：2026 年真正的主题

### 3.1 为什么：context rot 与注意力预算

Anthropic 引用的 needle-in-a-haystack 类研究显示：**随着上下文窗口 token 数增加，模型准确回忆其中信息的能力下降**。根因是 Transformer 的注意力是 O(n²)、训练数据偏短序列。结论：上下文是**有限资源、边际收益递减**的，每一个 token 都在消耗有限的注意力预算。

### 3.2 上下文的组成与工程优先级

| 组件 | 角色 | 工程建议 |
| --- | --- | --- |
| System prompt | 核心指令层 | 最小化、信息密度最大化；"黄金分割"高度——既非硬编码 if-else，也不空泛 |
| Tools | 与外部世界交互的接口 | 最小可行集合、自包含、错误鲁棒、描述极其详尽 |
| Examples | Few-shot 示例 | 精选且多样，而非堆砌边界情况（新模型上甚至应改用"设计接口"取代示例） |
| Message history | 对话历史 | 动态压缩（compaction）、工具结果清理 |
| External data | 运行时数据 | Just-in-time 动态加载，优于全量预加载 |
| Memory / Notes | 跨窗口状态 | 外部持久化，reset 后读回 |

总原则：**保持信息高信号，同时保持紧凑。**

### 3.3 检索范式：pre-inference vs just-in-time

Anthropic 描述的范式转移是：不再推理前把所有相关数据塞进上下文，而是**只保留轻量引用**（文件路径、查询语句、URL），由工具在恰当时机取回。Claude Code 用的是**混合模型**：`CLAUDE.md` 前置注入，`glob` / `grep` / `Bash` 负责即取即用，从而绕开索引过期与语法树复杂度问题。**元数据本身也是上下文信号**——目录层级、命名约定、时间戳。

### 3.4 长任务的四种武器

| 技术 | 机制 | 代价 / 注意 |
| --- | --- | --- |
| **Compaction** | 接近上限时把历史压缩为摘要，重开窗口继续 | 压缩过度会丢失"重要性稍后才显现"的细节；Claude Code 在约 70% 预算时触发，保留文件路径、决策、计划，丢弃冗余工具输出 |
| **Tool result clearing** | 清理深层历史中的工具原始结果 | 最轻量、最安全的压缩形式 |
| **Structured note-taking** | agent 主动把状态写入外部文件，reset 后读回 | Claude 玩 Pokémon 案例：跨数千步维持目标计数、区域地图、战斗策略 |
| **Sub-agent** | 焦点子任务在干净窗口中运行，只回传精炼摘要（约 1000–2000 token） | 主 agent 只需结论，不需原始证据 |

### 3.5 Anthropic 的新增项：context reset 而非 compaction

Anthropic 在《Harness design for long-running application development》中提出：模型会表现出 **"context anxiety"**——接近它以为的上下文上限时**提前收尾**。Compaction 保留连续性，但给不了干净起点，焦虑仍在。因此他们转向 **context reset**：清空上下文、启动全新 agent，用结构化交接物传递上一个 agent 的状态与下一步。代价是编排复杂度、token 开销与时延。

同一篇文章还给出第二个关键洞察：**agent 自评不可靠**——被要求评估自己产出时，模型倾向于自信地称赞平庸之作（主观任务尤甚）。解法是引入**生成者–评估者分离**（灵感来自 GAN）：planner / generator / evaluator 三 agent 架构，把"这是好设计吗"转译为可评分的准则。**训练一个苛刻的独立评估者，比让生成者变得自我批判容易得多。**

### 3.6 Claude 5 代模型的新上下文规则（2026 年最重要的单篇）

Anthropic 发现新代模型（Opus 5 / Fable 5）上大量旧规则已变成"神话"，于是把 Claude Code 的系统提示砍掉超过 80%：

| 维度 | 旧规则（Then） | 新规则（Now） |
| --- | --- | --- |
| 约束 | 给 Claude 硬规则（"默认不写注释""绝不写多段 docstring"） | **让 Claude 用判断力**（"写像周边代码一样的代码：匹配注释密度、命名、习惯用法"） |
| 工具 | 给使用示例引导探索空间 | **设计接口**：让参数更有表达力（如状态枚举 pending/in_progress/completed 本身就暗示用法） |
| 信息位置 | 全部前置（代码审查细则写进系统提示） | **渐进式披露**：验证与代码审查移入独立 skill，工具可延迟加载 |
| 重复 | 系统提示与工具描述重复强调（旧模型更听末尾指令） | **精简工具描述**：删掉重复示例，用法只写一处 |
| 记忆 | 手动 `#` 保存到 `CLAUDE.md` | **自动记忆**（auto-memory） |
| 规格 | 简单 markdown 计划文件 | **富引用**：HTML artifact、测试套件、真实代码库函数、rubrics |

配套机制：部分工具（如 Task 工具）改为 **deferred loading**，agent 必须先用 **ToolSearch** 检索完整定义才能调用——从而支持更多工具而不撑爆上下文。新命令 **`/doctor`（`claude doctor`）** 可自动"rightsize"你的 skills 与 `CLAUDE.md`。

**给自建 harness 的建议**：system prompt 紧密结合你的产品上下文（这是自建 agent 最该投入的地方）；`CLAUDE.md` 保持轻量，token 主要花在代码库"坑点"上，不写显而易见的事；长 skill 拆多文件并用渐进式披露；优先引用代码文件（HTML mockup 比文字描述或截图保真度高）。**总体动作只有一个词：简化。**

---

## 四、工具设计：Agent–Computer Interface

### 4.1 定位

Anthropic 的定义最精准：**工具是一种新型软件，体现"确定性系统与非确定性 agent"之间的契约。** `getWeather("NYC")` 每次结果相同；但 agent 何时调用、传什么参数、如何解读返回，全由模型决定。因此工具必须**为 agent 而设计**，而不是为开发者或系统设计。

### 4.2 Anthropic 的四条规则（官方文档口径一致）

1. **极其详尽的描述**——官方原话是"这是工具表现最重要的因素"。目标每工具 3–4 句：做什么、何时用、**何时不该用**、每个参数含义、重要限制与注意事项。
2. **合并同类操作**——不要 `create_pr` / `review_pr` / `merge_pr` 三个工具，而是一个工具加 `action` 参数。工具越多，选择歧义越大。
3. **有意义的名字空间**——`github_list_prs`、`slack_send_message`、`db_query`；前缀/后缀位置对不同模型效果不同，需实测。
4. **只返回高信号信息**——用语义化、稳定的标识符（slug / UUID），只返回模型决定下一步所需的字段，避免上下文膨胀。

一个反面/正面对照（Anthropic 官方示例）：

- 差：`"Gets the stock price for a ticker."`
- 好：`"Retrieves the current stock price for a given ticker symbol. The ticker symbol must be a valid symbol for a publicly traded company on a major US stock exchange like NYSE or NASDAQ. The tool will return the latest trade price in USD. It should be used when the user asks about the current or most recent price of a specific stock. It will not provide any other information about the stock or company."`

关键在最后一句——**明确说出工具不做什么**，能阻止模型"碰运气式"调用。

### 4.3 意图驱动的工具合并

判定标准：**如果人类工程师都无法确定某个场景该用哪个工具，就不能指望 agent 做得更好。**

| 反模式（按 API 端点切） | 推荐（按意图切） | 理由 |
| --- | --- | --- |
| `list_users` + `list_events` + `create_event` | `schedule_event` | agent 想"安排一件事"，不想做 CRUD |
| `read_logs` + `search_logs` | `search_logs` | 只返回相关行，而非数据洪流 |
| `get_customer_by_id` + `list_transactions` + `list_notes` | `get_customer_context` | 一次调用拿齐判断所需 |

### 4.4 工具数量与上下文成本

业界经验规则：**同时可用的工具最好少于 20 个，超过 10 个准确率就开始退化。** 现实风险：随手接几个 MCP server 就可能达到 **90+ 个工具定义、50,000+ token 的 JSON schema**，模型还没开始推理就先背上了负担。这直接催生了延迟加载（ToolSearch）与 Skills 的分层设计。

### 4.5 评估驱动的工具开发闭环

Anthropic 的方法论是一个三段式循环：

1. **原型**：把工具包成本地 MCP server，在 Claude Code / Desktop 里直接试。目标不是完美，而是拿到真实交互数据。
2. **评测**：**弱任务**是"单次调用、无分支"；**强任务**要求多次工具调用、真实数据、复杂工作流。例如把"下周和 Jane 开个会"升级为"下周和 Jane 开会讨论我们最新的 Acme 项目，附上上次项目规划会的纪要，并预订一间会议室"。指标：准确率、运行时、工具调用次数、token 消耗、工具报错数。**然后逐条读原始转录**——"agent 在反馈中省略的内容，往往比它写出来的更重要"。
3. **与 agent 协作迭代**：把失败转录喂给编码 agent（Claude Code）自动重构工具，用留出集防止对特定 prompt 过拟合。

---

## 五、Skills 与渐进式披露：2026 年最大的一次工程范式收敛

### 5.1 时间线

- **2025-10-16**：Anthropic 在 Claude 中推出 Skills（打包"如何做好一件事"的程序性知识）。
- **2025-12-18**：发布为**开放标准**（`SKILL.md`）。
- **数周内**：OpenAI Codex、Google Gemini CLI、GitHub Copilot、Cursor、VS Code、JetBrains Junie 等相继采纳。
- **2026 年**：40+ 产品支持；社区市场索引超过 40 万个 skill；Google ADK 内置 `SkillToolset` 实现完整三层披露（`list_skills` / `load_skill` / `load_skill_resource`）。

### 5.2 三层加载模型

| 层级 | 加载内容 | 成本量级 | 触发时机 |
| --- | --- | --- | --- |
| L1 发现（Advertise） | 仅 frontmatter 的 `name` + `description` | 约 **100 token / skill** | 常驻系统提示 |
| L2 激活（Load） | 完整 `SKILL.md` 正文 | 建议 < **5,000 token**（< 500 行） | 任务与 description 匹配时 |
| L3 深潜（Deep dive） | `references/` 文档、`scripts/` 脚本 | 按需，脚本只把**输出**送进上下文 | 执行阶段按需 |

对照效果：30 个专用工作流若全部前置约需 **150,000 token**；改为 Skills 后启动时约 **3,000 token**。路由**完全由模型前向推理完成**——没有 embedding、没有分类器、没有算法路由。**因此 `description` 是整个 skill 里最重要的字段。**

### 5.3 三层抽象的分工

| 层 | 抽象 | 解决什么 | 原始作者 |
| --- | --- | --- | --- |
| **MCP** | Tool / Resource / Prompt | agent 如何"**伸手**"拿外部能力与数据 | Anthropic |
| **A2A** | Agent Card / Task / Message / Artifact | agent 如何"**开口**"与其他 agent 协作 | Google |
| **Agent Skills** | `SKILL.md` + scripts/references | agent 如何"**记住怎么做**"某类工作 | Anthropic |

三者互补：skill 可以指示 agent 去用某个 MCP server、定义如何解读其输出、并在破坏性操作前强制安全检查；换 MCP server 不必改 skill，改 skill 不必动 MCP 配置。

### 5.4 写 skill 的实践要点

- **`description` 当路由逻辑写，不要当营销文案写**：明确回答"何时该用 / 何时不该用 / 产出是什么 / 成功标准是什么"。
- **加负样本**：一个反直觉的失败模式是——提供 skill 反而**先降低了正确触发率**。Glean 的生产案例：skill 路由在定向评测中触发率下降约 **20%**，补上"**不要在……时调用本 skill**"的负样本与边界情况后才恢复。
- **模板与示例放进 skill**：它们在未触发时是"免费的"；继续把模板塞进系统提示会拖累所有无关查询。Glean 报告这是他们生产中质量与延迟的最大收益来源之一。
- **确定性优先**：关键校验打包成脚本（代码是确定性的，语言解读不是）。写"执行 `python scripts/validate.py --input {filename}`"，而不是"验证数据"。
- **路径约定**：`.claude/skills/`（Claude Code）、`.agents/skills/`（**跨平台约定**）、`.gemini/skills/`、`.codex/skills/`。格式一致，路径不同。
- **注意客户端差异**：`disable-model-invocation` 是 Claude Code / VS Code 的扩展，不在核心规范内；Codex 用独立文件 `agents/openai.yaml` 中的 `policy.allow_implicit_invocation`。要跨客户端禁用自动调用，需两处都写。

### 5.5 Skills 的评测方法（OpenAI 2026-01 方法论）

核心洞察：迭代 skill 时你很难分辨"真的改善了"还是"只是改变了行为"。解法是把它当 LLM 应用来评。

**四类成功信号**：Outcome（任务完成？应用能跑？）、Process（是否调用了预期 skill / 走了预期步骤）、Style（产出是否符合约定）、Efficiency（是否无谓反复、token 是否合理）。

**Prompt set 设计（10–20 条起步）**，四类必须齐备：

| 类型 | 作用 | 示例 |
| --- | --- | --- |
| 显式触发 | 验证 skill 名称与指令可用 | "用 `$setup-demo-app` 创建项目" |
| 隐式触发 | 验证 `description` 是否精确 | "搭一个最小 React demo app 做 UI 实验" |
| 带噪触发 | 模拟真实业务上下文 | "创建一个小 demo 演示 Responses API" |
| **负样本** | 捕捉**误触发** | "给我现有的 React app 加 Tailwind"（不该触发建新项目） |

**执行与打分**：用 `codex exec --json` 捕获 JSONL 事件流（相当于飞行黑匣子），先做**确定性检查**（有没有执行 `npm install`、有没有创建 `package.json`、事件顺序对不对），再用 **结构化 rubric** 做第二阶段的只读评分——输出固定 schema（`overall_pass` / `score` / `checks[].id` / `pass` / `notes`），使模型评分可存储、可对比、可聚合，而不是另一段无法解析的主观意见。

**原则**：确定性规则守底线，rubric 管质量。不要一上来就把所有判断交给模型裁判。

---

## 六、记忆与状态

| 层级 | 机制 | 代表实现 |
| --- | --- | --- |
| 会话内 | session / thread 持久化 | Claude Agent SDK `session_id` + `resume`；Responses API `previous_response_id` |
| 跨会话长期记忆 | 文件型或平台托管 | Anthropic **memory tool**（Sonnet 4.5 起公开测试，基于文件系统）；Google **Memory Bank**；Claude 自动记忆 |
| 结构化笔记 | agent 主动写状态文件 | Claude 玩 Pokémon 的笔记文件；`NOTES.md` 模式 |
| 任务级状态 | 显式句柄 | MCP 去 session 后的官方推荐：由工具 mint 一个显式 handle，让模型作为参数在工具间传递 |

**关键工程点**：MCP 去掉协议级 session 后，官方明确建议"**让模型看得见句柄并在工具间穿线**"，这比把状态藏在传输层更好。同时，**记忆是新的攻击面**——"记忆与上下文投毒"已被 Anthropic 列为 agent 系统的五类主要威胁之一，写入路径必须受控。

---

## 七、评估与可观测性：从"看得到"到"测得准"

### 7.1 行业现状（LangChain 2026 State of Agent Engineering，1300+ 受访者）

| 指标 | 数值 |
| --- | --- |
| 已有 agent 在生产环境 | **57%**（10k+ 员工组织为 67%） |
| 已实施可观测性 | **89%** |
| 跑离线评测 | **52%** |
| 跑在线评测 | **37%** |
| 头号落地障碍 | **质量（32%）**；企业规模下**安全**升至第二（24.9%） |
| 主要评测方法 | 人工评审 **59.8%** |
| 成本作为障碍 | 显著下降（token 价格三年降幅约 99.7%） |

读法：**几乎人人装了摄像头，但只有一半在真正看录像。** 可视性带来的是"知道自己错过了什么"，而不是"变得更好了"。

### 7.2 三家实验室的评估方法论

**Anthropic — 尽早开始的 eval-driven development**
- 不必等数百条数据：从需求与真实失败中整理 **20–50 个任务**起步，每个任务写清输入、成功条件、允许与禁止的行为，并准备至少一个能通过评分器的参考解。
- 持续**阅读失败轨迹**，判断究竟是 agent 真失败，还是题目 / 环境 / 评分器有缺陷。
- 评估集是需要长期维护、有明确责任人的**产品资产**。
- 用 **pass^k**（连续 k 次全部成功）而非 pass@k 衡量生产可靠性：单次成功率 75% 时，"三次至少成功一次"约 98.4%，但"**连续三次全部成功**"只有约 **42.2%**。一次成功演示证明不了上线资质。

**OpenAI — Trace / Graders / Datasets / Eval Runs**
- **Trace** 是评估 agent 行为的第一性证据：一次运行中的模型生成、工具调用、guardrail、agent handoff、自定义事件都被记录；Span 对应一次模型调用或工具执行。
- 能回答输出评分答不出的问题：工具选对了吗？参数对吗？有没有无效重试？多 agent 是否在正确条件下交接？prompt/路由改动后端到端路径是否真的改善？
- **Graders** 三型：string-check（确定性精确/包含/正则）、Python（对整条轨迹做任意断言，最适合工具顺序、参数、步数预算）、model（LLM judge + rubric，用于主观质量）。
- **Macro-eval**（2026-05 开发者 Cookbook）：把分析单位从"单次失败"升级为"跨轨迹群体的模式"。四个标签——`case_type`（输入场景）、`run_outcome`（结局）、`eval_finding`（局部信号）、`behavior_pattern`（群体聚类）。做法是把每条 trace 压成紧凑文档，再做无监督聚类，产出**按频率与严重度排序的少量高价值模式清单**。它解决的问题是：**逐条看 trace 会优化"最响亮的失败"，而不是"最高频的失败"**——某定价 agent 在 12% 的运行中忽略激励信号，永远不会产生灾难性单点，却静默地劣化整体结果。

**Google — 三件套**
- **Agent Evaluation**：对线上运行表现评分。
- **Agent Observability**：实时追踪执行路径，快速定位与修复。
- **Agent Simulation**：部署前用合成交互做压力测试。

### 7.3 评估的三个层级与最小可用体系

**层级**：run（一次执行步骤）→ trace（一次执行的 run 序列）→ thread（多轮交互的 trace 组合）。**模式**：offline / online / ad hoc。

**三层最小体系**（推荐直接照搬）：
1. **触发评测**：覆盖"应该触发"与"不该触发"的 prompt（误触发通常比不触发更危险，因为它会改仓库、耗资源、污染工作区）。
2. **行为评测**：读 trace 中的工具调用、命令顺序、文件变更。
3. **产物评测**：构建 / 冒烟测试 + 结构化 rubric。

**CI 门禁**：在触及 prompt / 工具 / 模型的 PR 上触发评测，通过率低于阈值（如 0.95）即失败；确定性 grader 作硬门禁，model grader 用略宽阈值。

---

## 八、安全与治理：零信任 Agent

### 8.1 威胁面

Anthropic《Zero Trust for AI Agents》（2026-05-27）列出的 agent 系统主要威胁：

| 威胁 | 说明 |
| --- | --- |
| 直接提示注入 | 用户输入中覆写系统指令 |
| **间接提示注入** | 网页、邮件、文档等外部数据中嵌入指令 |
| 工具污染 | 用恶意工具替换合法工具；或"危险调用链"——单个安全的工具组合出风险行为 |
| 身份与权限滥用 | 具备合法权限的 agent 被诱导做不当操作 |
| 记忆与上下文投毒 | 污染长期记忆或上下文 |
| 供应链风险 | 依赖、MCP server 元数据、skill manifest 等 |

**Lethal trifecta**（Simon Willison）：当 agent 同时拥有①不可信输入、②联网能力、③访问私有信息时，就可被操纵去窃取并外泄数据。**最安全的做法是让三者永不同时出现；若必须同时具备，拆分到多个 agent 并用受控通道通信。**

### 8.2 2026 年的真实漏洞（Black Hat USA 2026，Novee 披露）

三家厂商**自己的仓库在默认配置下**被攻破：

| 厂商 | Agent | 主要风险 | 响应 |
| --- | --- | --- | --- |
| Anthropic | Claude Code | 通过 GitHub issue 的提示注入导致 RCE 与凭证窃取 | 多次补丁，分配 **CVE-2026-54316** |
| Google | Gemini CLI | 绕过执行限制，导致凭证窃取与潜在供应链风险 | 评为 **CVSS 10.0**，更改非交互环境的信任模型 |
| OpenAI | Codex | 可写 `AGENTS.md` 造成跨多个自动化阶段的**持久化提示注入** | 加固仓库流程，明确将 `AGENTS.md` 声明为**不可信输入面** |

根因不是模型行为，而是 **harness 层隐藏的信任假设**。研究者建议：用最小权限约束 agent、隔离工作流各阶段、不要让 agent 执行之间共享可写工作区、**把工作流生成的文件一律当不可信输入**、持续验证 agent 行为随依赖与默认配置的变化。

### 8.3 Anthropic 的三原则与三层成熟度

**三原则**：永不默认信任、假设一定会出问题（assume breach）、**最小代理权（least agency）**——不只是最小访问权限，还包括严格限制 agent 的**动作类型、调用频率与可触达范围**。

| 层级 | 要求 |
| --- | --- |
| 基础 | 每个 agent 实例唯一**加密身份**；短效访问令牌；默认拒绝 + RBAC；处理不可信输入（网页、文档）的 agent **沙箱执行实际上成为强制项**；自动化的初步事件分流 |
| 企业 | 完整的凭证生命周期管理；不可篡改审计日志；异常检测后自动限权 / 隔离 / 中止；**静态 API key 与共享服务账号即使在基础层也不可接受** |
| 高级 | mTLS 双向认证；**硬件绑定的身份（HSM / TPM）与远程证明**；机密计算环境；持续授权控制；异常后自动恢复 |

**可观测性指标**：详尽记录所有工具调用、数据访问与外部通信并送入 SIEM 实时关联；跟踪 **dwell time**（异常驻留时间）与**告警调查覆盖率**；关键系统的异常检测目标为**一小时内**；建立**可追溯矩阵**，把每个 agent 动作关联回原始请求并重建完整决策链。

**响应原则**：**把事件周围的事务性工作自动化，而不是自动化关键决策。** agent 可以做证据收集、初步分类、并行调查分支、起草复盘；**遏制、事件披露、客户沟通必须由人决定。**

**效果量化**：引用微软 Spotlighting 研究，间接提示注入成功率从 **>50% 降至 <2%**；Anthropic 自研 **constitutional classifiers** 阻断 **>95%** 的越狱尝试且误报增长极小。供应链侧建议 AI-BOM、OpenSSF Scorecard、依赖审计。

### 8.4 分层防御的工程骨架

| 层 | 做法 |
| --- | --- |
| 1. 输入分离 | 明确界定不可信输入面（邮件、网页、检索文档、仓库元数据、issue 标题、日志、**工具描述**、**skill manifest**、**远程 MCP 元数据**、聊天消息、记忆写入） |
| 2. 工具风险分级 | 按"读/写、可逆性、账号权限、下游影响"分级；用 policy-as-code 显式声明审批与出网策略 |
| 3. 动作校验 | 工具调用执行前，由独立校验层检查该动作是否与声明任务一致、是否在授权范围内——**不判断注入是否发生，只判断动作是否异常** |
| 4. 沙箱 | 进程隔离：agent 在一个上下文跑，工具在另一个上下文跑，只通过定义良好的接口共享最小表面 |
| 5. 运行时行为监控 | 把 agent 当不可信进程，监控**整个会话的动作序列**是否与声明目的相符（正常：读源码→发 PR 评论；异常：读源码→读 SSH 密钥→建出站连接） |

policy-as-code 示例（分级与审批）：

```yaml
tools:
  read_ticket:        { risk: low,      approval: none }
  create_draft_reply: { risk: medium,   approval: required_if_external_recipient }
  send_email:         { risk: high,     approval: always, outbound_allowlist: [corp.example, partner.example] }
  shell_exec:         { risk: critical, approval: always, runtime: sandbox_only, filesystem_scope: workspace_only, egress: deny }
```

**审批要保留意义**：Anthropic 自己的数据显示，**用户批准了 93% 的 Claude Code 权限提示**——人类审批会退化为仪式。解法不是取消审批，而是只在高后果边界上保留，把真正低风险的路径自动化。

### 8.5 沙箱技术选型

| 机制 | 隔离强度 | 冷启动 | 适用 |
| --- | --- | --- | --- |
| 容器（namespaces + cgroups） | 弱（共享内核，存在逃逸风险） | <500ms | 开发环境、低安全场景 |
| **MicroVM（Firecracker）** | 强（独立内核） | ~125ms，内存开销 ~5MB/VM | **生产级 agent 代码执行的首选** |
| gVisor（syscall 拦截） | 中（用户态内核，约 20% syscall 未实现） | 容器级 | 存量容器增强隔离，几乎无需改代码 |
| Kata Containers | 强（独立内核 + OCI 兼容） | 较高 | 需强隔离又保留 K8s 调度 |
| WASM | 强（线性内存 + WASI 白名单） | 微秒级 | 插件系统、边缘；Python 生态尚不成熟 |

商用/托管侧：E2B（Firecracker 底层，Agent 生态专用度最高）、Modal（函数级、支持 GPU）、Morph Cloud（快照/恢复，把冷启动降到毫秒级）、Daytona（完整开发环境）。

---

## 九、协议与互操作：三层栈在 2026 年定格

### 9.1 MCP：史上最大改版（2026-07-28）

由 Linux Foundation 下的 **Agentic AI Foundation (AAIF)** 治理。这是 MCP 发布二十个月以来最大的架构修订，主维护者的说法是"有人开玩笑叫它 v2，从精神上说是准确的"。

**核心变化**：

- **无状态协议核心**：正式废弃 `initialize` / `initialized` 握手与 `Mcp-Session-Id`。每个请求自带协议版本、客户端身份与能力（`_meta`），可落在**任意一个** server 实例上，配合普通轮询负载均衡即可水平扩展，不再需要粘性路由或共享会话存储。
- **`server/discover` RPC**：客户端想预先获知 server 能力时可调用，但非必需。
- **HTTP 原生**：方法名与工具名通过 `Mcp-Method` / `Mcp-Name` 头传递，网关可直接基于 header 做路由与鉴权。
- **Multi Round-Trip Requests (MRTR)** 取代 server 发起的请求，使 elicitation 等流程在无状态 server 上也能工作。
- **列表结果可缓存**（`ttlMs` / `cacheScope`），顺序确定，稳定上游 prompt cache。
- **正式扩展框架**：Tasks（长时异步任务）、MCP Apps（服务端渲染交互界面）、Enterprise-Managed Authorization (EMA)。
- **授权加固**：RFC 9207 issuer 校验、issuer-bound client credentials、**以 CIMD（Client ID Metadata Documents）取代动态客户端注册 DCR**。
- **正式废弃政策**：12 个月最低窗口。

**代价与规模**：传输层载荷变大（但高度可压缩）；生态已有 **10,000+ MCP server**；Tier 1 SDK 月下载量接近 5 亿次，TypeScript 与 Python SDK 累计下载量均突破 10 亿。

**路线图五大优先级**：agentic messaging primitives（server 主动事件 / webhooks / channels）、HTTP 原生传输统一与加固（含 HTTP over stdio、ETag）、**agent 身份与企业级安全**（DPoP、Workload Identity Federation、标准 token exchange——因为调用方越来越是"代表缺席用户、或向子 agent 授予更窄权限"的云端 agent）、以及扩展与治理成熟化。

### 9.2 A2A：从提案到产业级标准

- **2025-04-09** Google 在 Cloud Next 发布；一年后在 **Linux Foundation** 下成长为 **150+ 组织**参与、**六种语言 SDK**、**三大云平台（Google / Microsoft / AWS）原生集成**的标准。
- **2026-03-12 发布 v1.0**——首个稳定、生产就绪版本，由含 AWS、Cisco、Google、IBM Research、Microsoft、Salesforce、SAP、ServiceNow 八家公司代表的 TSC 治理。v1.0 的贡献是"**成熟化而非重造**"：应用协议定义与传输映射彻底解耦、收紧规范行为、OAuth 2.0 现代化（移除 implicit/password grant，新增 device code 与 PKCE）、原生化多租户、**Signed Agent Card 作为跨组织信任根基**。

**四项设计原则**：不透明执行（opacity，不暴露 prompt / 记忆 / 工具链）、能力导向发现（`/.well-known/agent-card.json`）、长任务是一等公民（9 种 `TaskState`、SSE 流式、Webhook 推送、Async First）、安全与治理内建。

**工程陷阱**（官方与社区共识）：
- **过早引入 A2A**：如果所有 agent 都在一个进程里，普通 sub-agent 比网络协议简单得多。**只在 agent 真正跨服务、跨团队时才用 A2A。**
- **假设远程 agent 可信**：A2A agent 是网络服务，**它的响应是不可信输入**，可能携带针对你模型的指令，必须校验与围栏化。
- **把 ADK 与 A2A 混淆**：ADK 是工具包，A2A 是协议；可以只用 ADK 不暴露 A2A，也可以从非 ADK agent 说 A2A。
- **忘记状态在 session 里**：工具函数通常无状态，需要跨轮共享的数据属于 session state。
- **多 agent 的 trace 会在 hop 处断裂**：需要显式的 hop span 类型（如 `A2A_CLIENT` / `A2A_SERVER`）与跨跳传递的 trace id，否则 dashboard 会说谎。

### 9.3 三层栈的分工总表

| 关注点 | MCP | A2A | Agent Skills |
| --- | --- | --- | --- |
| 交互对象 | 模型 ↔ 工具/数据源 | agent ↔ agent | agent ↔ 程序性知识 |
| 核心抽象 | Tool / Resource / Prompt | Agent Card / Task / Message / Artifact | `SKILL.md` + scripts/references |
| 通信方向 | 主要单向调用 | 双向协作（流式 + 异步通知） | 无通信，注入上下文 |
| 状态模型 | 无状态或有限状态 | 完整 Task 状态机 | 无状态文件 |
| 安全 | API key / OAuth | OAuth 2.0 / OIDC / mTLS / 签名 Agent Card | 供应链：manifest 与脚本需审计 |

---

## 十、经济学：成本降级为次要矛盾，质量升格为主要矛盾

- **token 价格崩塌**：GPT-4（2023-03）$30/$60 每百万 token → Gemini 3.1 Flash（2026-04）$0.10/$0.40，三年降幅约 **99.7%**。中端模型（$1.5–$3）已超越十八个月前的最强模型。
- **成本不再是头号障碍**：LangChain 2026 中成本关注度显著下降，**质量（32%）** 成为第一障碍。
- **但多智能体仍很贵**：约 **15 倍** token；BrowseComp 分析中 token 量解释 **80%** 性能方差——**很多看起来是"协调赢了"的效果，其实是"更多 token 赢了"。** 2026 年若干预印本进一步报告：在总思考 token 预算相等时，单 agent 在多跳推理上可持平或超过多 agent 方案。
- **模型路由是刚需**：探索类 subagent 路由到小模型、主会话留在大模型，仅此一项就可在调优前让探索部分成本降约 **5 倍**。GPT 侧的建议形态是"轻量模型对话 + 强模型承接重任务"。
- **可靠性瓶颈在基础设施**：Datadog 2026 报告显示，**限流错误占 LLM 调用失败的 60%**——原因不是厂商不可靠，而是 agent 循环、ReAct 式推理链与多 agent 交接制造了不可预测的并发尖峰。
- **工作量在扩张而非收缩**：Anthropic 报告指出约 **27%** 的 AI 辅助工作是原本不会存在的新任务。瓶颈从"工程产能"转向"**产品判断力**"——新的稀缺不是代码，而是知道哪些代码值得生成。

---

## 十一、跨实验室共识与分歧

### 11.1 十二条共识

1. 上下文工程 > 提示词工程；上下文是有限资源。
2. 默认单智能体，多智能体需论证。
3. 渐进式披露是上下文成本控制的第一原则。
4. 工具描述质量是最高杠杆的单点改进。
5. 工具数量要收敛，宁少勿多。
6. 长任务靠压缩 + 外部记忆 + 上下文隔离。
7. 评估对象是轨迹而非最终答案。
8. 可观测性必须闭环到评测，否则只是"知道错过了什么"。
9. 确定性检查优先于模型评分；模型裁判只用于主观维度。
10. Agent 默认不可信；边界放在动作上，放在沙箱与身份上。
11. 人机协同不是过渡态，而是当前的最佳形态。
12. 开放标准（MCP / A2A / Agent Skills）优于私有编排。

### 11.2 五处分歧

| 议题 | 一方 | 另一方 |
| --- | --- | --- |
| 多智能体的边界 | Anthropic：orchestrator–workers 有效，90.2% 提升，适合 breadth-first 研究 | Cognition：平行写动作会产生冲突的隐式决策；2026 修正为"**读可多，写单一**" |
| 长任务续接 | Anthropic：**context reset + 结构化交接**（compaction 无法消除 context anxiety） | 多数框架：以 compaction 为主，保留连续性 |
| 是否给示例 / CoT | Anthropic：新模型上**给示例反而限制探索**，应改为设计接口 | Google：结构化提示与模板仍有价值；但两者都反对强制冗长 CoT |
| 平台 vs 代码 | OpenAI：**裁撤可视化 Agent Builder 与 Evals 平台**（2026-11-30 关停），回归 Agents SDK 与代码优先 | Google：**反向加码平台化**（Gemini Enterprise Agent Platform 收拢全部 Vertex 服务） |
| 自评可行性 | Anthropic：agent 自评不可靠，必须生成者 / 评估者分离 | 部分产品：单 agent 内多轮自我反思仍是默认 |

---

## 十二、落地路线图（90 天）

**第 0–30 天：把地基打好**
- 选定**一个**高价值、可验证的用例（客户服务 26.5% / 研究分析 24.4% 是当前最主流的两个落点）。
- 做**单 agent + 工具**基线，不做多 agent。
- 收敛工具：合并同意图操作，工具数控制在 20 以内，逐个重写描述（3–4 句，含"何时不该用"）。
- 接入 trace 级可观测性，沉淀 20–50 条评测任务（含负样本与对抗样本）。
- 建立"成功"的可测量定义：结果 / 过程 / 风格 / 效率四类目标。

**第 31–60 天：把纪律装上**
- 上下文纪律：定义压缩触发阈值、工具结果清理、外部笔记文件约定。
- 把评测接进 CI：通过率低于阈值即阻断合并；确定性 grader 作硬门禁。
- 工具风险分级 + policy-as-code；对处理不可信输入的路径强制沙箱。
- 给 agent 独立加密身份与短效 token，废弃静态 API key 与共享账号。
- 盘点不可信输入面清单，明确"哪些输入面可以前置到有意义动作之前"。

**第 61–90 天：把规模打开**
- 只有在"可压缩到几行 / 超过 10 轮 / 可真并行 / 需要更专提示"时才引入 subagent；写动作保持单线程。
- 引入 macro-eval：定期对 trace 群体做聚类，找按频率与严重度排序的系统性模式。
- 建立成本与延迟预算：每任务最大 token / 最大工具调用 / 最大时长；探索类走小模型。
- 治理上线：agent 注册表、网关策略、审计轨迹、异常检测与自动遏制。
- 复盘一次"委托缺口"：哪些任务已经可以完全委托、哪些还不能，以及缺的是能力还是**可验证性**。

---

## 十三、风险与未解问题

1. **委托缺口是信心问题而非能力问题。** Anthropic 数据：开发者约 **60%** 的工作用 AI，但只有 **0–20%** 能完全委托。任务一旦变成设计密集或语义模糊，人就会收回控制——因为**无法验证的东西无法委托**。
2. **长任务的可验证性仍未被解决。** 主观任务（设计、文案）只能靠 rubric 代理，而 rubric 本身需要持续校准。
3. **多智能体的群体安全是空白区。** DeepMind 与 Anthropic 在同期各自发出警告；NIST 2026 年 3 月的大规模公开红队活动报告：400+ 参与者、25 万+ 次攻击尝试，**每一个目标前沿模型都至少被找到一次成功劫持**。
4. **协议碎片化风险。** 同一家企业在同一时间可能同时运行 MCP、A2A、Responses、Realtime、ADK 六种协议面，每个协议面需要不同的评测切面。
5. **供应商锁定 vs 开放标准。** 平台化（视觉编排、托管运行时）提升速度但加深锁定；开放标准（Skills / MCP / A2A）提升可移植性但把治理责任留给企业自己。
6. **记忆投毒与持久化后门。** 记忆写入路径是新的、尚未被充分防御的攻击面。
7. **"更多 token 赢"的错觉。** 若无法在等 token 预算下比较，多智能体的收益很可能是被误读的。

---

## 附录：核心来源

**Anthropic**
- Building effective agents（2024-12）
- Effective context engineering for AI agents（2025-09）
- Writing effective tools for agents — with agents（2026-01-29）
- Harness design for long-running application development
- The new rules of context engineering for Claude 5 generation models（2026-07-24）
- Zero Trust for AI Agents（2026-05-27）
- 2026 Agentic Coding Trends Report
- Claude Agent SDK / Claude Code 文档：Subagents、Hooks（27–30+ 事件节点）、Skills、Worktrees、`/doctor`

**OpenAI**
- A practical guide to building agents
- Building agents 学习路径（Responses API / Agents SDK / 模型选择）
- Shell + Skills + Compaction: Tips for long-running agents that do real work
- Testing Agent Skills Systematically with Evals（2026-01-22）
- Agent Evals / Trace grading / Evaluation best practices
- Macro-eval workflow（Developer Cookbook，2026-05-22）
- AgentKit（2025-10-06 DevDay）与 2026-06 的组件裁撤公告
- Guardrails（开源）、Lockdown Mode

**Google / DeepMind**
- Gemini API：Prompt design strategies（Agentic workflows 章节）、Function calling、Thought signatures、Tool combination（tool context circulation）
- Agent Development Kit (ADK) 与 A2A 协议规范
- Gemini Enterprise Agent Platform（2026-04-22 Cloud Next）：Agent Runtime / Memory Bank / Agent Identity / Registry / Gateway / Evaluation / Observability / Simulation
- QueryData 与 Context Engineering Assistant

**协议与标准**
- Model Context Protocol 规范 2026-07-28 与官方路线图
- A2A Protocol v1.0 规范（Linux Foundation）
- Agent Skills 开放标准（skill.md / agentskills.io）

**第三方生产数据与安全披露**
- LangChain, State of Agent Engineering 2026
- Datadog, 2026 State of AI Engineering
- Black Hat USA 2026（Novee）：Claude Code / Gemini CLI / Codex harness 漏洞
- NIST 大规模公开红队活动报告（2026-03）
