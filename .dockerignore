# ══════════════════════════════════════════════════════════════
# 构建上下文裁剪（合并后：server(Dockerfile.ghost) + worker(Dockerfile) + 舰队）
#
# ⚠️ 两个 Dockerfile 真正需要的输入不同，下面每条都注明了在排除谁的什么：
#    Dockerfile.base   —— kali 基础层，基本不读仓库内容
#    Dockerfile.ghost  —— 读 packages/{contracts,ghost} + web/ + main.py
#    Dockerfile        —— 读 packages/{contracts,ghost,worker} + skills/ + tools/ + web/
#    不要把 packages/ 或 web/ 排除掉。
# ══════════════════════════════════════════════════════════════

# ── 运行期产物：含各题 FLAG / MEMORY / SOURCE / _events.jsonl / _transcripts
#    既非构建输入，也不该随上下文上传（题号与靶标情报不进产物的口径）
work/
_transcripts/
pi/

# ── root 属主的运行数据：普通用户读不了，会让构建在"检查上下文"阶段直接失败
#    （error checking context: no permission to read from .../data/vpn/openvpn.log）
data/
vpn/

# ── 非构建输入的大目录（.git/.venv/前端源码 合计 50M+），纯拖慢构建
#    前端**源码与依赖**都不进上下文（构建输入是产物 web/，它不在这里排）
#    —— 用通配而非逐个点名：前端目录改过名（frontend → frontend-vue），
#    旧写法没跟上，整棵前端源码又回到了上下文里。
.git/
.venv/
legacy/
tsecbench/
tests/
frontend*/

# docs/reference/ 是参考知识库（第三方仓库的浅克隆，7.3M）—— 同上，非构建输入。
# 两个 Dockerfile 都不 COPY docs/，但排除它省下的是**每次构建**都要传一遍的上下文。
docs/reference/

# ── 上一轮镜像构建写进 src 的轮子缓存：**不排掉会被连带复制进镜像**，
#    而它含编译后的 .pyc —— 实测让"删掉的模块"继续可 import
#    （stale 缓存比"文件不存在"难排查得多）。三个包目录各一。
packages/*/build/
packages/*/dist/
packages/*/.venv/
packages/ghost/ghost.egg-info/
packages/contracts/ghost_contracts.egg-info/
packages/worker/ghost_worker.egg-info/

# ── 分叉归档：58MB，且内含活凭据
1.zip
*.zip
TsecBench-main/

# ── 凭据与环境文件：绝不进构建上下文
.agent.env
.agent.env.*
.env
.env.*

# ── Claude / 编辑器工作树
.claude/
.remember/

# ── 杂项
__pycache__/
**/__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
*.log
*.tmp
