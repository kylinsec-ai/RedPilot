[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "redpilot"
version = "0.1.0"
description = "RedPilot security benchmark platform: control plane + observability platform (unified)"
requires-python = ">=3.10"
# 基线零 fastapi:worker(Kali)镜像安装本发行版只为 redpilot.obs.localserver(stdlib),
# contracts 是唯一依赖 —— 与合并前的 redpilot-obs 基线设计一致(镜像瘦身为既定决策)。
# [platform] extra 供平台镜像拉起完整控制面 + 观测 API。
dependencies = ["redpilot-contracts"]

[project.optional-dependencies]
platform = ["fastapi", "uvicorn", "pydantic", "python-dotenv", "httpx"]

[tool.setuptools.packages.find]
# namespaces=false:每个包目录必须有 __init__.py。默认的命名空间模式会把
# 嵌套的自拷贝目录(history: redpilot/control/packages/redpilot/...)一并收进 wheel。
include = ["redpilot*"]
# 兜底:即便嵌套目录复现,也永不进 wheel。
exclude = ["redpilot.control.packages*", "redpilot.packages*"]
namespaces = false
