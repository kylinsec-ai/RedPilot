# ── 敏感配置（绝不提交）──
.env
.agent.env
.agent.env.*
*.env.local
*Zone.Identifier

# ── 运行产物 / 数据（含各题 FLAG、实录、VPN 状态）──
work/
data/
pi/
vpn/
*.ovpn
*.log
backend.log
frontend.log
fastapi.log
watchdog.log
supervisor_report.txt
django_console.sqlite3
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

# ── 分叉归档与跨分支残留 —— 含活凭据，绝不入库 ──
# 1.zip 内含: .env(BENCHMARK_TOKEN/SOLVER_API_KEY)、.agent.env(ANTHROPIC_AUTH_TOKEN)、
# vpn/*.ovpn、data/*.sqlite3 + 23MB fastapi.log + 两份 5.7MB 完整 Agent 转录。
1.zip
*.zip
TsecBench-main/

# ── 参考知识库 —— 第三方仓库的浅克隆，只在本机读，不入库 ──
# docs/reference/ 按需 clone 外部参考仓，当前一个:
#   all-agentic-architectures —— FareedKhan-dev/all-agentic-architectures
#   (35 个 agentic 架构的公开实现，MIT，--depth 1 快照 cf9d620 / 2026-05-28)
# 它们是只读参考材料，不是本仓内容: 体量可观、上游随时会变，且自带 src/ 与 tests/
# （正是下方那条 setuptools 命名空间发现事故的同款形状）。要归档再 clone 一次。
docs/reference/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# ── Node ──
node_modules/
dist/

# ── Python 构建产物（单发行版）──
/build/

# ── 系统 / 编辑器 ──
.DS_Store
*.swp
.playwright-mcp/

# ── 工作树 / 构建产物 ──
# bin/: Go 版 `make build` 的产物落在 bin/，但本分支是 Python 分支。
.claude/worktrees/
bin/

# 历史事故: control/ 下曾出现整棵 monorepo 的递归自拷贝，被 setuptools
# 命名空间发现收进 wheel 并污染 pytest 收集。此路径永不合法，兜底忽略。
redpilot/control/packages/
